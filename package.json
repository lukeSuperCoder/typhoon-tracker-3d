{
  "name": "typhoon-bavi-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "巴威台风实时路径追踪与三维可视化系统 · Cloudflare Worker + Mapbox GL JS",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "mapbox-gl": "^3.26.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "@types/geojson": "^7946.0.16",
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.86.0"
  }
}
